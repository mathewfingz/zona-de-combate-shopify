{%- capture class_badge -%}badge badge--bottom-left{%- endcapture -%}
{%- capture id_badge -%}NoMediaStandardBadge-{{ section_id }}-{{ card_product.id }}{%- endcapture -%}
{%- capture style_badge -%}--pl: {{ settings.padding_left }}px; --pr: {{ settings.padding_right }}px; --pt: {{ settings.padding_top }}px; --pb: {{ settings.padding_bottom }}px;{%- endcapture -%}

{%- if card_product.available == false -%}
    <span id="{{ id_badge }}" class="sold-out-badge {{ class_badge }}" aria-hidden="true" style="{{ style_badge }}">
        {{- 'products.product.sold_out' | t -}}
    </span>
{%- elsif card_product.compare_at_price > card_product.price and card_product.available -%}
    <span id="{{ id_badge }}" class="sale-badge {{ class_badge }}" aria-hidden="true" style="{{ style_badge }}">
        {{- 'products.product.on_sale' | t -}}
    </span>
{%- endif -%}
{%- if new_badge -%}
    <span id="{{ id_badge }}-new" class="new-badge {{ class_badge }}" aria-hidden="true" style="{{ style_badge }}">
        {{ 'products.product.new' | t }}
    </span>
{%- endif -%}
{%- if custom_badge -%}
    <span id="{{ id_badge }}-custom" class="custom-badge {{ class_badge }}" aria-hidden="true" style="{{ style_badge }}">
        {%- if custom_badge_text -%}
            {{ custom_badge_text }}
        {%- else -%}
            {{ 'products.product.custom' | t }}
        {%- endif -%}
    </span>
{%- endif -%}
